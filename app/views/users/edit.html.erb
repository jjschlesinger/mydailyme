<h1>Change your password</h1>
<div class="indent">
<%= error_messages_for :user %>

<% form_for(@user) do %>

<%= render :partial => 'form' %>
  
  <div>
    <%= submit_tag "Update" %>
  </div>

<% end %>
<%= link_to 'Back to Login', new_session_path %>
</div>
