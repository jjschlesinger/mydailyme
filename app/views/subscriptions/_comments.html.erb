  <a id="comments_link_<%=subscription.id%>" href="#" onclick="$('comments_<%=subscription.id%>').toggle();">Comments (<%= subscription.me.comments.count %>)</a>
  <div id="comments_<%=subscription.id%>" style="display:none; margin-left:10px;">
    <div id="comments_list_<%=subscription.id%>">
        <%= render :partial => 'comments/comment_list', :locals => { :comments => subscription.me.comments } %>
    </div>
    <div id="comment_add_<%=subscription.id%>">
    <% form_remote_tag :url => "/comments/create/#{subscription.id}" do -%>
      <div><%= text_field_tag "comment_text_#{subscription.id}" %><%= image_submit_tag ("plus.png") %></div>
    <% end -%>
    
    </div>
    <div id="comment_error_<%=subscription.id%>">
    	<%= error_message %>
    </div>
  </div>
