  <% if comments.length == 0 %>
      <span>No comments</span>
  <% else %>
    <% comments.each do |c|  %>
      <div>
        <span><%= c.user.display_name %>:</span> <span><%= c.message %></span>
      </div>
    <% end %>
  <% end %>